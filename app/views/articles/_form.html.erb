<br><br><br><br><br><br>
    <div class="container">
      <%= form_for @article, local: true do |form| %>
        <% if @article.errors.any? %>
            <div id="error_explanation">
              <h2>
                <%= pluralize(@article.errors.count, "Erros") %>, Este artigo não pode ser salvo
                pois existem erros !!!
                <% @article.errors.full_messages.each do |msg| %>
                  <li><%= msg %></li>
                <% end %>
              </h2>
              <ul>
              </ul>
            </div>
        <% end %>
            <p>
              <%= form.label :title %><br>
              <%= form.text_field :title %>
            </p>

            <p>
              <%= form.label :text %><br>
              <%= form.text_area :text %>
            </p>
            <p>
              <%= form.submit %>
            </p>
      <% end %>
    </div>
