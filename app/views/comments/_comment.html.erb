<p>
  <strong>Usuario:</strong>
  <%= comment.commenter %>
</p>

<p>
  <strong>Comentario:</strong>
  <%= comment.body %>
</p>

<p>
  <% if current_user.id == comment.user_id %>
    <%= link_to 'Apagar Comentario', article_comment_path(@article, comment),
    method: :delete,
    data: { confirm: 'Apagar o comentario ?' } %>
  <% end %>
</p>